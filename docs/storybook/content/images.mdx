import { Canvas, Meta } from '@storybook/addon-docs/blocks';

import { ReactCompareSlider, ReactCompareSliderImage } from 'react-compare-slider';

import { Images } from './stories/00-demos/00-index.stories'

<Meta title="Docs/Images" component={ReactCompareSlider} />

# Using Images

<Canvas of={Images} />

<br />

## `ReactCompareSliderImage`

Images can be rendered using the `ReactCompareSliderImage` component and/or
by using a custom image component, e.g. `picture` or `img`.

`ReactCompareSliderImage` is a standalone image (`img`) component which uses the
`styleFitContainer` utility for positioning and fitting. The defaults from `styleFitContainer`
are applied automatically but can be overridden using the `style` prop.

<br />

### `ReactCompareSliderImage` Props

`ReactCompareSliderImage` is a standard `img` component; it accepts all valid `img` props.

### Usage

```jsx
import { ReactCompareSliderImage } from 'react-compare-slider';

/** Standard usage. */
<ReactCompareSliderImage src="..." srcSet="..." />

/** Override `styleFitContainer` defaults. */
<ReactCompareSliderImage
  src="..."
  srcSet="..."
  style={{ objectPosition: 'left' }}
/>
```

## Live Examples

Checkout the [Images examples](/story/demos--images).
