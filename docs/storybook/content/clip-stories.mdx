import { ArgTypes, Canvas, Meta } from '@storybook/addon-docs/blocks';

import { ReactCompareSlider, ReactCompareSliderImage } from 'react-compare-slider';

import { Images } from './stories/00-demos/00-index.stories'

<Meta title="Docs/Clip" component={ReactCompareSlider} />

# Using `clip`

The `clip` prop allows you to control whether `itemOne`, `itemTwo`, or both use a clip mask to hide their content. This allows you to overlay `itemOne` on top of `itemTwo` or vice versa. This can be useful when items have transparent areas.

<Canvas of={Images} />

<ArgTypes of={ReactCompareSlider} />

<br />

## Live Examples

Checkout the [Images example](/story/demos--images).
